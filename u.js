const days = ['Даваа', 'Мягмар', 'Лхагва', 'Пүрэв', 'Баасан', 'Бямба', 'Ням'];
console.log('Сондгой өдөртэй гаригууд:');
for (let i = 0; i < days.length; i++) {
    if ((i+1) % 2 ===1) { // 1, 3, 5, 7, дахь өдрүүд
        console.log(days[i]);
    }
}