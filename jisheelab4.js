const prompt=require("prompt-sync")();
var arr=[];
var n, tooluur=0;
n=prompt("array bagana ug N ")
m=prompt("array mor ug M: ")
for(i = 0; i < arr.length; i++) {
    if (arr[i] % K === 0) {
        tooluur++;
    }
 }
 console.log(tooluur)